workflows:
  android-apk:
    name: StockHub Android APK
    max_build_duration: 30
    environment:
      java: 17
    scripts:
      - name: Build APK
        script: |
          chmod +x ./gradlew
          ./gradlew assembleDebug
    artifacts:
      - app/build/outputs/apk/debug/*.apk
